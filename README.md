# hrc-marketplace

A Claude plugin marketplace containing a single plugin, `hrc-sf`, which points at
an internal HRC Financial Group service.

The endpoint requires authorization and is not usable without it. This repository
contains configuration only — no data, no credentials, and no application code.
